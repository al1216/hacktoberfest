class Solution {
    public int lengthOfLastWord(String s) {
        s = s.replaceAll("\\s"," ");
        String[] t = s.split(" "); 
        System.out.println(Arrays.toString(t));
        return (t[t.length-1]).length();
    }
}
